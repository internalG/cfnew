[【保姆级教程】Cloudflare Pages V2.1 终极版！解决所有小白问题 (404/UUID/KV)，教你正确姿势！ - YouTube](https://www.youtube.com/watch?v=JhVxJChDL-E)

https://github.com/byJoey/cfnew

### **1. 准备阶段**
*   **下载项目文件**：访问视频下方的链接，进入 GitHub 的 Release 页面，找到并下载名为 `pages.zip` 的压缩文件。**注意：下载后的压缩包不需要解压**。
*   _worker.js 是主要部署文件：cp 少年你相信光吗 _worker.js && zip pages.zip _worker.js

### **2. 初始部署**
*   **创建 Pages 项目**：登录 Cloudflare 账号，进入 Workers 和 Pages 界面，点击“创建”并选择 **Pages** 下的“上传资产”（拖放文件）选项。
*   **上传文件**：为项目起一个名字，然后直接将下载的 `pages.zip` 文件拖入上传区域，点击“部署”并继续。
*   这个时候已经可以访问项目首页了，但是**设置uuid会出错，必须继续配置！**
*   **（可选）绑定自定义域名**：在部署成功页面点击“自定义域”，输入你自己的域名进行绑定。

### **3. 配置环境变量与数据库**
*   **设置 UUID 保护项目**：进入项目的“设置” -> “变量和机密”，添加变量名称为小写 **`u`**，值为生成的 UUID（URL进入项目需要）。
*   **绑定 KV 命名空间**：
    *   如果之前没有创建过 KV，需先到 Cloudflare 的“存储与数据库” -> “KV”中创建一个命名空间（名字随意）。
    *   回到 Pages 项目设置的“绑定”部分，添加 KV 命名空间绑定。变量名必须设为大写的 **`C`**，并选择刚才创建的命名空间。

### **4. 激活配置（关键步骤）**
*   **重新创建部署**：由于初始配置可能不会立即生效，需要回到“部署”选项卡，点击右上角的**“创建新部署”**。
*   **再次上传并保存**：重新将 `pages.zip` 文件拖入并点击“保存并部署”，此时之前的变量和 KV 配置才会正式生效。
*   **成功访问 https://prjname.pages.dev/u-u-i-d**

### **5. 优化与网络设置**
*   **开启 gRPC**：如果需要使用某些特定协议，需回到 Cloudflare 首页，进入你的**自定义域名**设置，在“网络”选项中找到 **gRPC** 并将其开启。
*   **高级设置**：在项目的配置页面，可以根据需要关闭“启用优选域名”或“启用 GitHub 优选”等选项，以解决部分节点不通的问题。
*   **小白建议**：对于自定义 IP 或优选 IP 列表等复杂选项，建议保持默认不填，直接使用推荐设置即可。

部署完成后，你可以通过“域名/UUID”的格式访问该页面，若能正常显示界面则表示部署成功。
